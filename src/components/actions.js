import React,{useState} from 'react'
const actions = async (action,objectType,id, currentUser)=> {
    if(action==='delete' && id)
    console.log('deleting..')
    return new Promise((resolve,reject)=>{
        
    })
}
export default actions